spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: ${REDIS_HOST:redis-17342.c54.ap-northeast-1-2.ec2.cloud.redislabs.com}
      port: ${REDIS_PORT:17342}
      username: ${REDIS_USERNAME:default}
      password: ${REDIS_PASSWORD:ng9szhywoeyXnjCxCw20XUUmYRcMgfJP}

logging:
  level:
    root: INFO
    com.otaku.community: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG

server:
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: true
